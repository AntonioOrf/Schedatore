{
  "name": "archiview",
  "version": "1.1.3",
  "description": "Gestionale per manoscritti medievali",
  "author": "Antonio Orf",
  "repository": {
    "type": "git",
    "url": "https://github.com/AntonioOrf/ArchiView.git"
  },
  "main": "out/main/main.js",
  "scripts": {
    "build-css": "npx @tailwindcss/cli -i ./src/renderer/css/input.css -o ./src/renderer/css/tailwind.css --minify",
    "copy-assets": "shx mkdir -p out/renderer && shx cp -r src/renderer/index.html src/renderer/css src/renderer/vendor out/renderer/",
    "extract": "lingui extract",
    "compile": "lingui compile",
    "build-ts": "npm run compile && tsc && npx esbuild src/renderer/js/logic/i18n.ts --bundle --outfile=out/renderer/js/logic/i18n.js --allow-overwrite && npm run copy-assets",
    "start": "npm run build-css && npm run build-ts && electron .",
    "pack": "npm run build-css && npm run build-ts && electron-builder --win",
    "publish-app": "npm run build-css && npm run build-ts && electron-builder --win -p always"
  },
  "build": {
    "generateUpdatesFilesForAllChannels": true,
    "appId": "com.antonioorf.archiview",
    "productName": "ArchiView",
    "directories": {
      "output": "dist"
    },
    "files": [
      "out/**/*",
      "package.json"
    ],
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "AntonioOrf",
        "repo": "ArchiView",
        "releaseType": "release"
      }
    ]
  },
  "devDependencies": {
    "@lingui/cli": "^6.1.0",
    "@lingui/conf": "^6.1.0",
    "@lingui/format-po": "^6.1.0",
    "@tailwindcss/cli": "^4.3.0",
    "@types/archiver": "^7.0.0",
    "@types/node": "^25.9.1",
    "electron": "39.8.5",
    "electron-builder": "^26.8.1",
    "esbuild": "^0.28.0",
    "make-plural": "^8.1.0",
    "shx": "^0.4.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@lingui/core": "^6.1.0",
    "archiver": "^8.0.0",
    "cors": "^2.8.6",
    "electron-updater": "^6.8.3",
    "express": "^5.2.1",
    "googleapis": "^173.0.0"
  }
}
